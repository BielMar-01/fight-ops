# ADR-005 — Conteúdo Público Dinâmico

## Status
Aceito

Textos, identidade visual e SEO precisam ser alteráveis sem deploy.

Decisão: persistir em `SiteSetting`, `PublicPage`, `PublicPageSection` e `SeoSetting`.

Um painel administrativo poderá editar esses dados futuramente.
